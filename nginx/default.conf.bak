server {
    listen 8111;
    server_name vps7.rockxi.ru:8111;

    location / {
        return 301 https://docs.google.com/presentation/d/1xg4UlE0TSuZZxatJ_5bg8lWIL9B9DzjGOMn5iEKyYT0/edit?usp=sharing;
    }

}
server {
    listen 80;
    server_name kurs.rockxi.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }

}

server {
    listen 443 ssl;
    http2 on;
    server_name kurs.rockxi.ru;

    ssl_certificate /etc/letsencrypt/live/kurs.rockxi.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kurs.rockxi.ru/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    error_page 404 500 502 503 504 /error.html;

    location = /error.html {
        root /usr/share/nginx/html;
        internal;
    }

    location / {
        proxy_pass http://localhost:8081/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 80;
    server_name couch.rockxi.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }

}

server {
    listen 443 ssl;
    http2 on;
    server_name couch.rockxi.ru;

    ssl_certificate /etc/letsencrypt/live/couch.rockxi.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/couch.rockxi.ru/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    error_page 404 500 502 503 504 /error.html;

    location = /error_couch.html {
        root /usr/share/nginx/html;
        internal;
    }

    location / {
        client_max_body_size 100M;
        proxy_pass http://localhost:5984/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 80;
    server_name cybersync.rockxi.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location /ws/ {
        proxy_pass http://localhost:6767;
        proxy_http_version 1.1;

        # WebSocket заголовки
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Стандартные заголовки
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Важно для WebSocket аутентификации
        proxy_set_header Origin http://$host;

        # Таймауты
        proxy_connect_timeout 7d;
        proxy_send_timeout 7d;
        proxy_read_timeout 7d;
    }

    location / {
        return 301 https://$host$request_uri;
    }

}

server {
    listen 443 ssl;
    http2 on;
    server_name cybersync.rockxi.ru;

    ssl_certificate /etc/letsencrypt/live/cybersync.rockxi.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/cybersync.rockxi.ru/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    error_page 404 500 502 503 504 /error.html;

    location = /error_couch.html {
        root /usr/share/nginx/html;
        internal;
    }

    location / {
        client_max_body_size 100M;
        proxy_pass http://localhost:6767/;

        # Заголовки для WebSocket
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Стандартные proxy заголовки
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        # Таймауты для долгих соединений
        proxy_read_timeout 86400;
    }
}

server {
    listen 80;
    server_name sklearn.rockxi.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }

}
server {
    listen 443 ssl;
    http2 on;
    server_name sklearn.rockxi.ru;

    ssl_certificate /etc/letsencrypt/live/sklearn.rockxi.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sklearn.rockxi.ru/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    error_page 404 500 502 503 504 /error.html;

    location = /error.html {
        root /usr/share/nginx/html;
        internal;
    }

    location / {
        proxy_pass http://localhost:8010/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 80;
    server_name affine.rockxi.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
server {
    listen 443 ssl http2;
    http2 on;
    server_name affine.rockxi.ru;

    ssl_certificate /etc/letsencrypt/live/affine.rockxi.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/affine.rockxi.ru/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;


    access_log /var/log/nginx/affine_access.log;
    error_log /var/log/nginx/affine_error.log;

    location / {
        proxy_pass http://localhost:3010/;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        client_max_body_size 100M;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
        proxy_buffering off;
    }
}






server {
    listen 443 ssl;
    http2 on;
    server_name sklearn.rockxi.ru;

    ssl_certificate /etc/letsencrypt/live/sklearn.rockxi.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sklearn.rockxi.ru/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    error_page 404 500 502 503 504 /error.html;

    location = /error.html {
        root /usr/share/nginx/html;
        internal;
    }

    location / {
        proxy_pass http://localhost:8010/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}






server {
    listen 80;
    server_name uinx.rockxi.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name uinx.rockxi.ru;

    ssl_certificate /etc/letsencrypt/live/uinx.rockxi.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/uinx.rockxi.ru/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    error_page 404 500 502 503 504 /error.html;

    location = /error.html {
        root /usr/share/nginx/html;
        internal;
    }

    location / {
        proxy_pass http://localhost:1337;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # WebSocket Support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
